# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NFCカード開発システム - 環境変数設定サンプル
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# このファイルを `.env.local` にコピーして、実際の値を設定してください。
# コマンド: cp .env.local.example .env.local
#
# ⚠️ 注意: .env.local は .gitignore に含まれており、リポジトリにコミットされません。
#          機密情報（APIキー、シークレット等）は必ず .env.local に記載してください。
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ============================================================================
# 必須設定
# ============================================================================

# ----------------------------------------------------------------------------
# Supabase設定
# ----------------------------------------------------------------------------
# Supabaseプロジェクトの設定画面から取得してください。
# 場所: Supabaseダッシュボード > Project Settings > API

# SupabaseプロジェクトのURL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key（公開キー）
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key（サーバー専用・機密情報）
# ⚠️ このキーはクライアント側には絶対に公開しないでください
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------------------
# サイト識別子
# ----------------------------------------------------------------------------
# 複数サイトを管理する場合に使用します（例: site-001, site-002）
# 単一サイトの場合は任意の識別子を設定してください
SITE_ID=your-site-id-here


# ============================================================================
# セキュリティ設定（推奨）
# ============================================================================

# ----------------------------------------------------------------------------
# Kiosk API認証
# ----------------------------------------------------------------------------
# 入退室キオスク端末からのAPI呼び出しを保護するためのシークレット
# ランダムな文字列を生成してください（例: openssl rand -hex 32）
# 
# 本番環境では必ず設定してください！
# 未設定の場合、認証なしでKiosk APIにアクセス可能になります（セキュリティリスク）
KIOSK_API_SECRET=your-kiosk-secret-here

# クライアントサイド（キオスク画面）から使用するシークレット
# KIOSK_API_SECRETと同じ値を設定してください
# ⚠️ この値はクライアントサイドに公開されますが、キオスク端末は物理的に保護された環境で使用されることを前提としています
NEXT_PUBLIC_KIOSK_API_SECRET=your-kiosk-secret-here

# ----------------------------------------------------------------------------
# CRON API認証（オプション）
# ----------------------------------------------------------------------------
# 外部CRONサービスから自動退室APIを呼び出す場合に使用
# ※ 通常は不要: 入退室処理のたびに自動退室チェックが実行されます
# 
# 外部CRONサービスを使用する場合のみ設定してください
# CRON_API_SECRET=your-cron-secret-here


# ============================================================================
# LINE連携設定（オプション）
# ============================================================================
# LINE通知機能を使用する場合に設定してください
# LINE Developers Console から取得: https://developers.line.biz/console/

# ----------------------------------------------------------------------------
# LINE Messaging API設定
# ----------------------------------------------------------------------------
# LINEチャネルのChannel Secret
LINE_CHANNEL_SECRET=your-line-channel-secret-here

# LINEチャネルのChannel Access Token (Long-lived)
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token-here

# LINE公式アカウントのURL（紐づけ用QRコード生成に使用）
# LINE Developers Console > チャネル基本設定 > 公式アカウントURL
# または、LINE公式アカウントマネージャー > 設定 > 基本情報 > URL
# 例: https://line.me/R/ti/p/@your-official-account-id
NEXT_PUBLIC_LINE_OFFICIAL_ACCOUNT_URL=https://line.me/R/ti/p/@your-official-account-id


# ============================================================================
# デプロイメント設定
# ============================================================================

# ----------------------------------------------------------------------------
# アプリケーションベースURL
# ----------------------------------------------------------------------------
# 本番環境のURL（LINE連携のリダイレクトURLなどに使用）
# Vercelにデプロイする場合は自動的に設定されます
# 
# ローカル開発環境: http://localhost:3001
# 本番環境（例）: https://your-app.vercel.app
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# ----------------------------------------------------------------------------
# Vercel環境変数（自動設定）
# ----------------------------------------------------------------------------
# Vercelにデプロイすると自動的に設定されます。手動設定は不要です。
# VERCEL_URL=your-app.vercel.app


# ============================================================================
# 開発環境設定
# ============================================================================

# 環境識別子（development, production, test）
# Next.jsによって自動的に設定されます。通常は変更不要です。
# NODE_ENV=development


# ============================================================================
# 設定完了後のチェックリスト
# ============================================================================
#
# ✅ Supabase設定（NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY）
# ✅ SITE_ID
# ✅ KIOSK_API_SECRET（本番環境では必須）
# ✅ NEXT_PUBLIC_KIOSK_API_SECRET（KIOSK_API_SECRETと同じ値を設定）
# ✅ LINE設定（LINE通知機能を使用する場合）
# ✅ NEXT_PUBLIC_BASE_URL
#
# 設定後、以下のコマンドでアプリケーションを起動してください：
# npm run dev
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
